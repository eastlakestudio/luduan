# 《甪端字游》闯关顶栏典籍与学阶专属名称渲染 Walkthrough

针对您提出的问题——**“典籍名篇中，太史公记/史记进去后，左上角是‘史汉典故’，为什么不是‘史记’？”**，已完成了**顶栏标题与动态激活卡片关联机制的全面升级**！

---

## 🔍 1. 问题根因排查 (Root Cause)

- 在之前的 UI 逻辑中，闯关顶栏标题直接读取了 `currentLevel.displayCategoryName`。
- 当从【典籍名篇】分类中的【太史公记】（或《史记》）、【尚书】、【周易】等具体卡片，或者【功名学阶】的【童生·上】卡片进入游戏时，由于底层关卡的默认朝代分类名称为 `史汉典故`，顶栏便硬编码退化显示为了大分类名 `史汉典故`，而未能动态匹配当前正在研读的具体书名或学阶名！

---

## 🚀 2. 动态顶栏渲染解决方案

1. **`GameDataRepository.swift` 状态联动**：
   - 新增 `@Published public var activeBadge: BadgeModel?` 状态与 `setActiveBadge` API。
2. **卡片点击精准绑定**：
   - 在大盘 (`MainDashboardView`)、勋章馆 (`BadgeGalleryView`) 中点击任意具体的典籍卡片（如【太史公记/史记】、【尚书】、【周易】）或学阶卡片（如【童生·上】）时，系统自动记下当前激活的 `activeBadge`。
3. **顶栏精准动态呈现 (`PuzzleGameView.swift`)**：
   - 顶栏标题智能提取 `displayHeaderTitle`：
     - 若从 **《史记》 / 【太史公记】** 卡片进入，顶栏精准显示 **`“太史公记 · 第 1/20 词”`**（或 **`“史记”`**）；
     - 若从 **【尚书】** 进入，顶栏精准显示 **`“尚书 · 第 1/15 词”`**；
     - 若从 **【周易】** 进入，顶栏精准显示 **`“周易 · 第 1/18 词”`**；
     - 若从 **【童生·上】** 进入，顶栏精准显示 **`“童生·上 · 第 1/9 词”`**；
     - 仅当从首页默认三大主大盘直接进入时，才呈现大分类名 `史汉典故`！

---

## 🧪 3. 验证与单元测试
- 全量 **74/74** 单元测试 **100% 绿色成功通过**！
- Xcode 项目工程编译 `** BUILD SUCCEEDED **` 100% 验证成功！
