# 《甪端字游》【典籍名篇】勋章卡片去重与统一合并 Walkthrough

针对您反馈截图中的疑问——**“诗经风雅和诗经两个为什么要独立？”**，已完成了【典籍名篇】列表下重复卡片的**清洗、合并与统一归一化**！

---

## 🔍 1. 问题根因排查

- 在 [`badges.json`](file:///Users/minghualiu/personal/EastlakeStudio/luDuan/Sources/core/Resources/badges.json) 数据集中，历史版本同时保留了两个对应《诗经》的勋章配置：
  1. `badge_shijing`：**【诗经雅韵】**（带彩绘精美插画，图片 `badge_shijing`）；
  2. `badge_class_1`：**【诗经】**（带通用朱印占位图，图片 `badge_class_1`）。
- 因为两者同属于 `"category": "典籍名篇"` 分类，导致界面上同时并排渲染出了两张一模一样的《诗经》典籍卡片（如您截图所示）。

---

## 🚀 2. 统一合并解决方案

1. **剔除冗余重复项**：
   - 从 `badges.json` 中正式**删除了重复的 `badge_class_1`（诗经）占位卡片**。
2. **统一唯一权威典籍卡片**：
   - 保留拥有精美古风名家彩绘插图的 **【诗经雅韵】** 作为《诗经》在【典籍名篇】分类中的**唯一权威卡片**。
   - `theme_books.json` 中 `badge_shijing` 直连《诗经》离散种子库 `shijing.json`。

**效果**：
在【典籍名篇】列表中，**《诗经》现在只有一张唯一的【诗经雅韵】精美卡片**，彻底消除了冗余重复，界面干净整洁！

---

## 🧪 3. 验证与单元测试
- 全量 **74/74** 单元测试 **100% 绿色成功通过**！
- Xcode 项目工程编译 `** BUILD SUCCEEDED **` 100% 验证成功！
